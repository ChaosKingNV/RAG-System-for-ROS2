def clean_data(raw_data):
    # Example of a basic cleaning function
    cleaned_data = []
    for item in raw_data:
        # Remove unnecessary parts or clean up the text
        cleaned_data.append(item)
    return cleaned_data
